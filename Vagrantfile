# -*- mode: ruby -*-
# vi: set ft=ruby :

#==========================================================
# To disable an specific language comment the "load" line
#==========================================================
begin
  load 'gui/Vagrantfile'
  load 'python/Vagrantfile'
  load 'nodejs/Vagrantfile'
  load 'php/Vagrantfile'

rescue LoadError
  puts "Error loading Vagrantfile(s)"
end
